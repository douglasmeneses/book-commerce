services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      DATABASE_URL: postgres://postgres:password@localhost:5432/bookcommerce
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev
